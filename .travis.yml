language: ruby
rvm:
  - "2.1.0"
before_install:
  - mysql -e "create database IF NOT EXISTS see_and_shop_test;" -uroot
before_script:
  - rake --trace db:create test
  - rake --trace db:migrate test
# uncomment this line if your project needs to run something other than `rake`:
script: bundle exec rspec spec